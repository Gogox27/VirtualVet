import { View, Text } from "react-native";
import Feather from "@expo/vector-icons/Feather";
import React from "react";

const Edit = (props) => {
  return <Feather name="edit-3" size={20} color="black" strokeWidth={2.5} />;
};

export default Edit;
