import { View, Text } from "react-native";
import React from "react";
import { Ionicons } from "@expo/vector-icons";

const Person = (props) => {
  return (
    <Ionicons name="person-outline" size={26} color="black" strokeWidht={2.5} />
  );
};

export default Person;
